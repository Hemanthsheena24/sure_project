📋 SHOPKART SETUP CHECKLIST
═════════════════════════════════════════════════════════════════

PRE-REQUISITES
══════════════════════════════════════════════════════════════════

✓ Docker installed and running
  Command: docker --version
  Expected: Docker version 20.x or higher

✓ Docker Compose installed
  Command: docker-compose --version
  Expected: Docker Compose version 2.x or higher

✓ Git (optional, for version control)
  Command: git --version
  Expected: Git version 2.x or higher


INSTALLATION & SETUP
══════════════════════════════════════════════════════════════════

✓ Navigate to project directory
  Command: cd trying
  Verify: You should see backend/, frontend/, and docker-compose.yaml

✓ Verify project structure
  - backend/ folder exists with server.js, models/, routes/
  - frontend/ folder exists with HTML files
  - docker-compose.yaml file exists
  - Dockerfile in both backend/ and frontend/

✓ Check backend/.env file
  File: backend/.env
  Should contain:
    - MONGO_URI=mongodb://admin:password123@mongo:27017/shopkart?authSource=admin
    - PORT=5000
    - JWT_SECRET=your_jwt_secret_key_here

✓ Verify backend/package.json
  File: backend/package.json
  Should have:
    - "start": "node server.js"
    - "dev": "nodemon server.js"
    - "seed": "node scripts/seedDatabase.js"


STARTING THE APPLICATION
══════════════════════════════════════════════════════════════════

✓ Step 1: Start all services
  Command: docker-compose up -d
  Wait: 30-60 seconds for containers to start
  Expected: No errors in output

✓ Step 2: Verify containers are running
  Command: docker-compose ps
  Expected Output:
    - shopkart-frontend    (running)
    - shopkart-backend     (running)
    - shopkart-mongo       (running)
    - shopkart-mongo-express (running)

✓ Step 3: Check container health
  Command: docker-compose ps
  All STATUS columns should show "Up"

✓ Step 4: View logs if any issues
  Command: docker-compose logs
  Should see: "MongoDB Connected" and "Server running on port 5000"


VERIFICATION & TESTING
══════════════════════════════════════════════════════════════════

✓ Test Frontend
  URL: http://localhost:8080
  Expected: ShopKart website loads
  Verify: Can see home page, products, navigation

✓ Test Backend Health
  URL: http://localhost:5000/api/health
  Command: curl http://localhost:5000/api/health
  Expected: {"status": "Server is running", "timestamp": "..."}

✓ Test MongoDB Connection
  Command: docker-compose logs backend | grep "MongoDB Connected"
  Expected: "MongoDB Connected" message

✓ Test Mongo Express
  URL: http://localhost:8081
  Login: admin / admin
  Expected: Database admin interface loads, can see "shopkart" database

✓ Test API - Get Products
  URL: http://localhost:5000/api/products
  Command: curl http://localhost:5000/api/products
  Expected: Returns empty array [] or product list

✓ Test API - Register User
  Command: curl -X POST http://localhost:5000/api/users/register \
    -H "Content-Type: application/json" \
    -d '{"name":"Test","email":"test@example.com","password":"test123"}'
  Expected: Returns user object with token

✓ Test Database - View Collections
  Access: http://localhost:8081
  Navigate: shopkart database
  Expected: Can see Users, Products, Carts, Orders collections


LOAD SAMPLE DATA (OPTIONAL)
══════════════════════════════════════════════════════════════════

✓ Seed Database with Sample Products
  Command: docker-compose exec backend npm run seed
  Expected: "Sample products inserted successfully"

✓ Verify Sample Data
  Command: curl http://localhost:5000/api/products
  Expected: 6 products returned (Smartphone, Headphones, SmartWatch, TV, Laptop, Earbuds)


DEVELOPMENT MODE
══════════════════════════════════════════════════════════════════

✓ Backend Auto-Reload
  Command: Edit any file in backend/
  Wait: 2-3 seconds
  Check: docker-compose logs backend | grep "restarted"
  Expected: Backend automatically reloads

✓ Frontend Hot Reload
  Command: Edit any HTML/CSS/JS file in frontend/
  Refresh: Browser (Ctrl+R or Cmd+R)
  Expected: Changes appear in browser


TROUBLESHOOTING CHECKS
══════════════════════════════════════════════════════════════════

✓ All containers running?
  Command: docker-compose ps
  Fix: If stopped, run: docker-compose up -d

✓ Port conflicts?
  Command: netstat -ano | findstr :8080 (Windows)
           lsof -i :8080 (Mac/Linux)
  Fix: Change port in docker-compose.yaml

✓ Database connection issues?
  Command: docker-compose logs mongo
  Fix: Restart: docker-compose restart mongo

✓ API not responding?
  Command: curl http://localhost:5000/api/health
  Fix: Check backend logs: docker-compose logs backend

✓ Frontend not loading?
  Command: curl http://localhost:8080 -I
  Fix: Restart frontend: docker-compose restart frontend


COMMON COMMANDS REFERENCE
══════════════════════════════════════════════════════════════════

✓ View All Logs
  Command: docker-compose logs -f
  Use: Monitor all services in real-time

✓ View Specific Service Logs
  Command: docker-compose logs -f backend
  Use: Debug specific container

✓ Stop All Services
  Command: docker-compose down
  Use: Gracefully stop containers

✓ Stop and Remove Everything
  Command: docker-compose down -v
  Use: Fresh start, removes volumes

✓ Rebuild Containers
  Command: docker-compose build
  Use: After changing Dockerfile or dependencies

✓ Execute Command in Container
  Command: docker-compose exec backend sh
  Use: Access container shell for debugging

✓ View Container Logs
  Command: docker-compose logs backend --tail 50
  Use: See last 50 lines of logs


DATABASE VERIFICATION
══════════════════════════════════════════════════════════════════

✓ Access MongoDB CLI
  Command: docker-compose exec mongo mongosh -u admin -p password123
  Inside shell:
    - use shopkart
    - db.users.find()
    - db.products.find()
    - exit

✓ Check Database Collections
  Command: docker-compose exec mongo mongosh
  In shell: use shopkart; show collections
  Expected: carts, orders, products, users

✓ Count Documents
  Command: db.users.countDocuments()
           db.products.countDocuments()
  Expected: Number of records in each collection


PERFORMANCE & MONITORING
══════════════════════════════════════════════════════════════════

✓ Check Container Resource Usage
  Command: docker stats
  Monitor: CPU, Memory usage of containers

✓ View Container Processes
  Command: docker-compose top backend
  See: What processes running inside container

✓ Disk Usage
  Command: docker system df
  Shows: Images, containers, volumes disk usage


SECURITY CHECKLIST
══════════════════════════════════════════════════════════════════

✓ Change MongoDB Password (Production)
  File: docker-compose.yaml
  Change: MONGO_INITDB_ROOT_PASSWORD: password123
  Change: ME_CONFIG_MONGODB_ADMINPASSWORD: password123

✓ Change Mongo Express Password (Production)
  File: docker-compose.yaml
  Change: ME_CONFIG_BASICAUTH_PASSWORD: admin

✓ Change JWT Secret (Production)
  File: backend/.env
  Change: JWT_SECRET=random_long_string_here

✓ Enable HTTPS (Production)
  Setup: Use nginx reverse proxy with SSL
  Consider: Use Let's Encrypt for free SSL certificates


BACKUP & RECOVERY
══════════════════════════════════════════════════════════════════

✓ Backup MongoDB Data
  Command: docker-compose exec mongo \
    mongodump --out /data/dump -u admin -p password123 --authenticationDatabase admin

✓ View MongoDB Volumes
  Command: docker volume ls | grep shopkart
  Expected: mongo_data and mongo_config volumes listed

✓ Manual Backup
  Command: docker-compose exec mongo tar -czf backup.tar.gz /data/db

✓ Restore from Backup
  Stop services, remove volumes, restore from backup


DEPLOYMENT PREPARATION
══════════════════════════════════════════════════════════════════

Before moving to production:

✓ Change all default passwords
✓ Update environment variables
✓ Set NODE_ENV=production
✓ Disable debug logging
✓ Configure proper CORS origins
✓ Set up SSL/TLS certificates
✓ Configure reverse proxy (nginx)
✓ Set up monitoring and alerts
✓ Configure automated backups
✓ Document all configurations
✓ Test disaster recovery
✓ Load test the application
✓ Set up CI/CD pipeline


FINAL VERIFICATION CHECKLIST
══════════════════════════════════════════════════════════════════

✓ All 4 containers are running and healthy
✓ Frontend loads at http://localhost:8080
✓ API responds at http://localhost:5000/api/health
✓ Mongo Express accessible at http://localhost:8081
✓ Can create users via /api/users/register
✓ Can view products via /api/products
✓ Can add items to cart via /api/cart/:userId
✓ Can create orders via /api/orders/:userId
✓ Database contains expected collections
✓ Sample data exists (if seeded)
✓ All logs show no errors
✓ Backend auto-reloads on file changes
✓ CORS headers are present in API responses
✓ MongoDB authentication works
✓ No port conflicts


QUICK REFERENCE - MOST USED COMMANDS
══════════════════════════════════════════════════════════════════

Start:         docker-compose up -d
Stop:          docker-compose down
Check Status:  docker-compose ps
View Logs:     docker-compose logs -f
Restart:       docker-compose restart
Rebuild:       docker-compose build && docker-compose up -d
Shell Access:  docker-compose exec backend sh
Database CLI:  docker-compose exec mongo mongosh


NEXT STEPS AFTER SETUP
══════════════════════════════════════════════════════════════════

1. ✓ Read README.md for project overview
2. ✓ Review ARCHITECTURE.html for system design
3. ✓ Check API_DOCS.md for detailed API documentation
4. ✓ Try API_EXAMPLES.sh for sample API calls
5. ✓ Integrate frontend with backend API
6. ✓ Add more features (search, filters, admin panel)
7. ✓ Set up authentication middleware
8. ✓ Implement payment gateway
9. ✓ Deploy to production
10. ✓ Set up monitoring and logging


SUPPORT & HELP
══════════════════════════════════════════════════════════════════

If something doesn't work:

1. Check docker-compose logs -f for errors
2. Review STARTUP.md troubleshooting section
3. Verify all prerequisites are installed
4. Ensure ports 8080, 5000, 8081, 27017 are available
5. Try: docker-compose down -v && docker-compose up -d
6. Check MongoDB is healthy: docker-compose ps mongo


═════════════════════════════════════════════════════════════════════════════

Created: 2024-11-11
Last Updated: 2024-11-11
Status: Ready for Production Setup

═════════════════════════════════════════════════════════════════════════════
